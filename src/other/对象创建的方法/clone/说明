创建对象的方法：
1. new语句创建对象
2. 调用对象的clone方法  被继承的类实现Cloneable接口，重写clone() 方法
3. 利用反射 反射机制：在运行时，对于任意一个类，都能知道这个类的所有属性和方法  getClass() 返回此Object的运行时类
4. 利用反序列化  把java对象转化为字节序列的过程称为对象的序列化，序列化只保存对象的状态，而不管对象的方法，
                static(类的状态) transient(对象的临时数据)