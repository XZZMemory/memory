package algorithm;

public class Floor {
    public int[][] getFloor(int i, int j) {
        int[] a;
        return null;
    }
    /**
     *
     int main() { //a个球，b次测试，所能测试到的最大楼层个数dp[i][j]
     int res=0,i=0;//n代表输入楼层数
     int a, n;
     cin >> a >> n;
     vector<int> v;
     for (int i = 0; i < a; ++i)
     v.push_back(1);
     for ( i = 1; v[a-1]<n; ++i) {
     for (int j = a-1; j > 0; --j) {
     v[j] = v[j] + v[j - 1] + 1;
     }
     v[0] = i + 1;
     }
     res = i;
     cout << res;
     //int i1 = v[a - 1][i - 1] + 1+1;//第1个分割点，需要二维
     return 0;

     ---------------------
     版权声明：本文为CSDN博主「NaNa_yzj」的原创文章，遵循CC 4.0 by-sa版权协议，转载请附上原文出处链接及本声明。
     原文链接：https://blog.csdn.net/NaNa_yzj/article/details/89088021
     */
}
